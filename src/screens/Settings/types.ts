import { DrawerScreenProps } from '@react-navigation/drawer';

import { DrawerParamList } from '@routes/types';

type ScreenProps = DrawerScreenProps<DrawerParamList, 'Settings'>;

export type SettingsProps = ScreenProps
